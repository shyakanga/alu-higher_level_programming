# Python - Everything is Object

This directory contains answers and exercises covering Python's object model: mutability, variable assignment, memory identifiers (`id`), and object comparison operators (`==` vs `is`).
