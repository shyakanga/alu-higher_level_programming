# Python - Network #1

This repository contains Python scripts that make HTTP requests using `urllib` and `requests` packages, handling response headers, body data, status codes, JSON parsing, and HTTP Basic Authentication.
