# Python - Input/Output

This project covers file handling in Python (`open`, `with`), JSON serialization/deserialization (`json.dumps`, `json.loads`, `json.dump`, `json.load`), command line arguments with `sys.argv`, object attribute representation (`__dict__`), and Pascal's Triangle implementation.
