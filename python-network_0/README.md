# Python - Network #0

This project introduces network programming using Bash scripts and `curl` to interact with HTTP servers, inspect headers, handle HTTP methods (GET, POST, DELETE, OPTIONS), and send custom headers and parameters.
