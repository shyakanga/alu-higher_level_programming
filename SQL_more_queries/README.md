# SQL - More Queries

This project covers advanced SQL database management using MySQL 8.0, including managing user permissions and privileges, constraints (`NOT NULL`, `DEFAULT`, `UNIQUE`, `PRIMARY KEY`, `FOREIGN KEY`), subqueries, `JOIN` statements (`INNER JOIN`, `LEFT JOIN`), and aggregate functions with `GROUP BY`.
