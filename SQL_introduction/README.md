# SQL - Introduction

This project covers basic database management using MySQL 8.0, including database creation and deletion, table definitions, CRUD operations, aggregation functions (`COUNT`, `AVG`), filtering (`WHERE`), and grouping (`GROUP BY`, `ORDER BY`).
